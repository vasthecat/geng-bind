[package]
name = "geng-bind"
version = "0.1.0"
edition = "2021"
links = "nauty"
build = "build.rs"

[build-dependencies]
cc = "1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
